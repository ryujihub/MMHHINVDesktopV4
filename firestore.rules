rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users to read/write all collections for now
    // This enables initial setup and can be refined later for production
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
